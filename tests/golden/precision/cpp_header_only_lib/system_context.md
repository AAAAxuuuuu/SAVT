# 系统上下文 (L1)

## 发起入口
- **App** (procedure_centric)：作为项目的启动入口，负责初始化运行环境并将控制权交给核心模块。

## 核心能力
- **图数据管理 — Ui** (🧱 核心数据结构)：负责加载、管理节点与边等图结构数据，是后续算法和业务分析的底层数据来源。

## 后台支撑
- **图数据管理 — Lib** (🧱 核心数据结构)：负责加载、管理节点与边等图结构数据，是后续算法和业务分析的底层数据来源。

