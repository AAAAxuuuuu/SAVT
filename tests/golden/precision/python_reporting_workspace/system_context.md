# 系统上下文 (L1)

## 发起入口
- **Tools** (💾 数据存储 (Database))：负责将数据持久化保存到本地数据库或文件中。

## 后台支撑
- **Analytics** (🎨 界面展示 (UI))：负责渲染界面和处理用户的点击/滑动等操作。
- **Data** (💾 数据存储 (Database))：负责将数据持久化保存到本地数据库或文件中。
- **Storage** (💾 数据存储 (Database))：负责将数据持久化保存到本地数据库或文件中。
- **Support** (tooling)：构建或预处理阶段的辅助模块，在主运行时流程之外完成转换、验证或生成工作。

